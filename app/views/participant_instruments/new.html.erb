<%= params %>
<%= @participant.name %>
<%= @options %>

<%= form_with(model: [@participant, @participant_instrument]) do |f| %>
  <%= f.label :instrument_id %>
  <%= collection_select :participant_instrument, :instrument_id, @options, :id, :name %>

  <%= f.submit t(:assign) %>
<% end %>

